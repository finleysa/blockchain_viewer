let insert = function(db, data, callback) {
  // Get the documents collection
  let collection = db.collection('coin_market_cap');
  // Insert some documents
  collection.insertMany(data
  , function(err, result) {
    if (err) callback(err);
    callback(result);
  });
}

module.exports = insert;
